{
  "name": "cursorsaudi-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "pnpm run build && wrangler dev",
    "fetch-dal-photos": "node scripts/fetch-dal-contributor-photos.mjs",
    "deploy": "pnpm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.8",
    "@astrojs/cloudflare": "12",
    "@astrojs/sitemap": "^3.2.1",
    "@tailwindcss/vite": "^4.1.11",
    "astro": "^5.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.8.0"
  },
  "devDependencies": {
    "wrangler": "^4.84.0"
  }
}
